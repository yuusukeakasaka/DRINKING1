<h1>マイページ</h1>
<h3><%= @user.name %></h3>
 <%= attachment_image_tag @user, :icon_image, :fill, 100, 100, format: 'jpeg', fallback: "no_image.png" %>
 <%= @user.introduction %>

 <% if @user.id == current_user.id %>
 <%= link_to "プロフィール編集", edit_user_path(@user) %>
 <% end %>
