<div class="container-fluid">
   <div class="row">
    <div class="col-md-12 my-3"></div>
  <div class="col-md-10 my-3 text-center"><h3>リモート飲み募集</h3></div>
  <div class="col-md-10 text-center">ここでは様々なサイトなどでリモート飲みを<br> 楽しむための募集を募る掲示板です。
                                     <br>リンクを貼って他のサイトなどで<br>お楽しみください。
  </div>
  <div class="col-md-10 my-3 text-center"><%= link_to "募集する", new_recruitment_path %></div>
  </div>

  <div class="row">
    <div class="col-md-1"></div>
     <div class="col-md-8 text-center">
      <% @recruitments.each do |recruitment| %>
       <table class="table table-borderless">
        <tbody class="card h-30">
         <tr>
          <td class="col-md-1">名前</td>
          <td class="col-md-1"><%= recruitment.user.name %></td>
         </tr>
         <tr>
          <td class="col-md-1">募集内容</td>
          <td class="col-md-1"><%= recruitment.recruitment_post %></td>
         </tr>
         <tr>
          <td></td>
          <td>
          <% if recruitment.user == current_user %>
          <%= link_to "削除", recruitment_path(recruitment), method: :delete %>
          <% end %>
          </td>
         <% end %>
        </tbody>
      </table>
    <div class="row">
    <div class="col-md-12 textcenter"><h4 class="text-center"><%= paginate(@recruitments) %></h4></div>
   </div>
     </div>
    </div>
</div>
